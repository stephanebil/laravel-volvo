<div class="max-w-sm mt-12 mx-4">
    <img src="img/img_show/{{ $arrayCard["img"] }}" alt="{{ $arrayCard["title"] }}">
    <div class="py-12">
        <p class="font-bold text-xl">{{ $arrayCard["title"] }}</p>
        <p class="text-gray-500">{{ $arrayCard["description"] }}</p>
    </div>
</div>